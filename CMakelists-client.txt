cmake_minimum_required(VERSION 3.8)

project(GameNetworkClient)

# Debug Mode
# set(CMAKE_BUILD_TYPE "Debug")
# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g")

file(GLOB_RECURSE SOURCES src/*.cpp)

list(APPEND INCLUDE_LIST E:/CLibs/asio-1.30.2/include) 
list(APPEND INCLUDE_LIST E:/CLibs/jsoncpp-1.9.6/include) 
list(APPEND INCLUDE_LIST include)

list(APPEND LIB_LIST ws2_32)
list(APPEND LIB_LIST wsock32)

message(STATUS ${INCLUDE_LIST})
message(STATUS ${LIB_LIST})

include_directories(${INCLUDE_LIST})

add_executable(${PROJECT_NAME} E:/CLibs/jsoncpp-1.9.6/include/jsoncpp.cpp src/simple-client-server/client.cpp)

target_link_libraries(${PROJECT_NAME} PRIVATE ${LIB_LIST})